name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            configure_preset: config-vs2022
            build_preset: build-vs2022-release
            test_preset: test-vs2022-release
          - os: ubuntu-latest
            configure_preset: config-gcc-release
            build_preset: build-gcc-release
            test_preset: test-gcc-release
          - os: ubuntu-latest
            configure_preset: config-clang-release
            build_preset: build-clang-release
            test_preset: test-clang-release

    steps:
    - uses: actions/checkout@v4

    - name: Install LLVM and Clang
      if: contains(matrix.configure_preset, 'clang')
      uses: KyleMayes/install-llvm-action@v2
      with:
        version: "18"
        env: true
        cached: true

    - name: Install Ninja and ccache (Linux)
      if: runner.os == 'Linux'
      run: sudo apt-get install -y ninja-build ccache

    - name: Configure CMake
      run: >
        cmake --preset ${{ matrix.configure_preset }}
        ${{ contains(matrix.configure_preset, 'clang') && '-DCMAKE_CXX_FLAGS=-fuse-ld=lld' || '' }}

    - name: Build
      run: cmake --build --preset ${{ matrix.build_preset }} --target either-tests

    - name: Test
      run: ctest --preset ${{ matrix.test_preset }} -j8 --output-on-failure

  sanitizers:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ASan
            configure_preset: config-clang-debug-asan
            build_preset: build-clang-debug-asan
            test_preset: test-clang-debug-asan
          - name: TSan
            configure_preset: config-clang-debug-tsan
            build_preset: build-clang-debug-tsan
            test_preset: test-clang-debug-tsan

    name: Clang Debug ${{ matrix.name }}

    steps:
    - uses: actions/checkout@v4

    - name: Install LLVM and Clang
      uses: KyleMayes/install-llvm-action@v2
      with:
        version: "18"
        env: true
        cached: true

    - name: Install Ninja and ccache
      run: sudo apt-get install -y ninja-build ccache

    - name: Configure CMake
      run: cmake --preset ${{ matrix.configure_preset }}

    - name: Build
      run: cmake --build --preset ${{ matrix.build_preset }} --target either-tests

    - name: Test
      run: ctest --preset ${{ matrix.test_preset }} -j8 --output-on-failure
